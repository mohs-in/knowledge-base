**Follow-up:**

> "What’s the difference between rest and spread syntax?"

**What to say:**

- **Rest** collects values into an array: `function(...args)`.
- **Spread** expands values: `Math.max(...arr)`.

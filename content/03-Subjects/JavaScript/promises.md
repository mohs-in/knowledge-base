**Follow-up:**

> "Can you explain how Promises work and how they help with async code?"

**What to say:**

- Promises represent a value that may be available now, later, or never.
- Avoids callback hell.
- Use `.then()` and `.catch()` or `async/await`.

**Follow-up:**

> "Can you explain the difference between `var`, `let`, and `const` with examples?"

**What to say:**

- `var` is function-scoped and hoisted.
- `let` and `const` are block-scoped and not hoisted in the same way.
- `const` cannot be reassigned, but its contents (like arrays/objects) can be mutated.

What is a key attribute?
It is nothing but a primary key that we studied earlier.

Typically we represent it by an underline.

![[Key Attribute Representation.png]]

As you can see in the diagram attached `cid` and `pid` are key attributes of `Customer` and `Product` respectively.
What is Multi-Valued Attribute?

As the name suggests it can have multiple values.

Example:

![[Multi Valued Attributes Image.png]]

Here, `phone no.` and `credit card` can be referred to as multi value attributes.
Because, a customer can have multiple phones numbers and credit cards.

We represent them with double ellipses.
